# VMNexus 新功能建议

> 难度标注：🟢 简单 | 🟡 中等 | 🔴 困难
> 基于当前代码实际实现情况分析

---

## 一、虚拟机管理增强

### 1. 快照与状态保存 — 🟡 中等（基础已有，需完善）
- ✅ 已有：`ctxTakeSnapshot` / `ctxRestoreSnapshot` / `ctxDeleteSnapshot` 已实现（基于 qemu-img snapshot）
- ✅ 已有：`autoSnapshotTimer` 自动快照已在 ConsoleWindowController 中实现
- ❌ 待做：快照管理器 UI（当前只是简单的 PopUpButton 选择，需要独立的快照列表视图，含时间线、缩略图预览）
- ❌ 待做：挂起/恢复（savevm/loadvm）— 需要 QEMU monitor 完整交互，当前 monitor 命令是 stdin 写入，无可靠返回解析
- ❌ 待做：关闭应用后下次启动直接恢复状态 — 需要在 AppDelegate 中记录上次运行 VM 并自动 savevm

### 2. 虚拟机克隆 — 🟢 简单（已基本完成）
- ✅ 已有：`ctxFullClone` 和 `ctxLinkedClone` 都已实现，通过 qemu-img convert/create 完成
- ❌ 待做：克隆向导 UI（当前是直接执行，没有让用户选择是否复制网络配置等的选项）— 加一个简单对话框即可

### 3. 虚拟机导入/导出 — 🟡 中等（基础已有，OVF/OVA 解析较复杂）
- ✅ 已有：拖拽导入 .vmnexus / .qcow2 / .vmdk / .iso / .ovf 已实现
- ✅ 已有：`ctxExportOVF` 导出基础 OVF XML 已实现
- ❌ 待做：完整的 OVF/OVA 解析导入（当前只是创建空 VM，未解析 OVF XML 中的磁盘引用、网络配置等）— OVF XML 解析工作量中等
- ❌ 待做：`.vmnexus` 打包分享格式（将配置+磁盘打包成单文件）— 用 zip 归档即可，🟢 简单

### 4. 虚拟机分组与标签 — 🟢 简单（已基本完成）
- ✅ 已有：`group` 属性、`ctxSetGroup` 已实现
- ✅ 已有：`isFavorite` 收藏功能已实现
- ✅ 已有：排序按 group → favorite → name
- ❌ 待做：自定义标签（Tag）系统 — 在 VM model 加 tags 数组 + 侧边栏标签筛选 UI
- ❌ 待做：侧边栏按分组折叠显示（当前是平铺列表）— 需要 NSTableView group row

---

## 二、网络功能增强

### 5. 虚拟网络编辑器 — 🟡 中等（UI 已有框架，底层复杂）
- ✅ 已有：`VMNexusNetworkEditorController` 独立控制器已存在
- ✅ 已有：VM model 中 `portForwards`、`bridgeInterface`、`hostOnlySubnet`、`dhcpRangeStart/End` 等属性齐全
- ❌ 待做：多虚拟交换机可视化编辑 — UI 工作量中等
- ❌ 待做：虚拟机之间组网互通 — 需要 QEMU socket/netdev 配置，中等

### 6. 端口转发管理 — 🟢 简单（模型已有）
- ✅ 已有：`portForwards` 数组 [{proto, hostPort, guestPort, guestIP}] 已在 model 中定义
- ❌ 待做：在 ConfigWindowController 中加一个端口转发列表编辑 UI — 纯 UI 工作
- ❌ 待做：将 portForwards 转换为 QEMU 的 `hostfwd` 参数 — 当前 buildQEMUArguments 只有一个随机 SSH 转发

---

## 三、显示与交互增强

### 7. 全屏与无缝模式 — 🟡 中等
- ✅ 已有：`toggleFullscreen` 调用 `window.toggleFullScreen` — 基本全屏已有
- ❌ 待做：自动隐藏工具栏的全屏模式 — 🟢 简单
- ❌ 待做：无缝/融合模式（Coherence Mode）— 🔴 非常困难，需要捕获虚拟机窗口并将其作为独立 macOS 窗口显示，涉及 QEMU display backend 深度改造
- ❌ 待做：多显示器支持 — model 中 `monitorCount` 已有，QEMU 参数已生成多 virtio-gpu，但实际显示需要多窗口管理

### 8. 分辨率自适应 — 🔴 困难
- ❌ 需要 QEMU virtio-vga 支持 guest agent 配合（spice-vdagent 或 QXL），当前 VNC 连接方式下无法动态改变 guest 分辨率
- ❌ HiDPI 整数倍放大：当前 `enableHiDPI` 是 bool 标志但没有实现实际的缩放逻辑 — 需要在 VNC viewer 中实现缩放渲染

### 9. 剪贴板共享 — 🔴 困难
- ✅ model 中 `enableSharedClipboard` / `enableDragDrop` 已有
- ✅ QEMU 参数已生成 virtio-serial + spice-vdagent
- ❌ 但实际实现需要 guest 中安装 spice-vdagent，且宿主机端需要监听 spice channel 并桥接到 macOS NSPasteboard — 涉及 IPC 协议实现
- ❌ 文件拖放同理，需要 spice-webdavd 或自定义协议 — 工程量很大

---

## 四、性能与资源管理

### 10. 性能监控仪表盘 — 🟢 简单（已基本完成）
- ✅ 已有：`VMNexusResourceMonitor` (Swift) 已实现 CPU、内存、磁盘 I/O、网络 I/O 实时监控
- ✅ 已有：Console 窗口 "Monitor" tab 已集成
- ❌ 待做：迷你图表显示在主窗口侧栏 — 在主窗口加一个小型化的 monitor view 即可
- ❌ 待做：历史数据回顾 — 需要持久化采样数据 + 图表绘制，🟡 中等

### 11. 资源限制与调度 — 🟡 中等
- ✅ 已有：`performanceProfile` (power-save/balanced/performance) 属性已定义
- ❌ 待做：将 performanceProfile 转换为实际的 cgroups/nice/CPU affinity 设置 — macOS 没有 cgroups，需要用 `setpriority` 和 `NSTask.qualityOfService`
- ❌ 待做：后台自动降低优先级 — 监听 `NSApplicationDidResignActiveNotification` 然后 `setpriority(PRIO_DARWIN_ROLE)` — 🟢 简单

---

## 五、存储与磁盘

### 12. 磁盘管理工具 — 🟡 中等
- ❌ 待做：磁盘扩容 — 调用 `qemu-img resize` 即可，🟢 简单
- ❌ 待做：磁盘压缩 — 调用 `qemu-img convert -c`（压缩输出），🟢 简单
- ❌ 待做：磁盘格式转换 — 调用 `qemu-img convert -O format`，🟢 简单
- ❌ 待做：多磁盘热插拔 — 需要 QEMU monitor `device_add` / `device_del` 命令，🟡 中等
- 以上都是调用 qemu-img，主要是 UI 工作量

### 13. USB 设备直通 — 🔴 困难
- ❌ 需要枚举 macOS IOKit USB 设备列表
- ❌ 需要 QEMU `-device usb-host,vendorid=,productid=` 参数
- ❌ 自动连接规则需要 IOKit notification 监听设备插拔
- ❌ 需要沙盒权限（entitlements）来访问 USB 设备

---

## 六、自动化与高级功能

### 14. 批量操作 — 🟢 简单
- ❌ 待做：`tableView.allowsMultipleSelection = YES` + 批量遍历执行操作
- ❌ 待做：批量配置修改 — 循环设置属性 + saveToBundle

### 15. 自动启动与计划任务 — 🟡 中等
- ❌ 待做：自动启动 — 在 `applicationDidFinishLaunching` 中检查 VM 的 `autoStart` 标志并启动，🟢 简单
- ❌ 待做：定时任务 — 需要 NSTimer 或 macOS LaunchAgent，加 UI 配置界面，🟡 中等

### 16. 远程管理 — 🔴 困难
- ❌ 待做：内建 Web 控制台 — 需要内嵌 HTTP server（如 GCDWebServer）+ WebSocket + VNC-to-Web 转发
- ❌ 待做：VNC 密码保护 — 当前 VNC 端口无密码，需要 QEMU `-vnc :0,password` + monitor `change vnc password` — 🟢 简单
- 整体 Web 控制台工程量大

---

## 七、用户体验优化

### 17. 快速预览 — 🟡 中等
- ✅ 已有：`captureScreenshotToPath` 和 VNC 截图能力
- ❌ 待做：鼠标悬停时显示缩略图 — 需要 NSTrackingArea + 定期截图缓存 + NSPopover/tooltip 自定义视图
- ❌ 待做：定时截取画面保存为缩略图 — 在运行中每 N 秒保存一次 preview.png 到 bundle

### 18. 搜索与过滤 — 🟢 简单（已基本完成）
- ✅ 已有：`searchField` + `controlTextDidChange` 搜索已实现（按 name 和 architecture 过滤）
- ❌ 待做：增加按 OS 类型、运行状态过滤 — 在搜索逻辑中加条件即可

### 19. 操作确认与撤销 — 🟢 简单
- ✅ 已有：删除前有 NSAlert 确认对话框
- ❌ 待做：移至回收站而非直接删除 — 用 `[[NSWorkspace sharedWorkspace] recycleURLs:]` 替代 `removeItemAtPath` — 一行代码
- ❌ 待做：Undo 撤销 — 需要 NSUndoManager 集成，🟡 中等

### 20. 新手引导 — 🟡 中等
- ❌ 待做：首次启动交互式引导 — 需要一组引导页面 UI（NSWindow + 步骤指示器）
- ❌ 待做：操作系统模板 — 预设常见 OS 的 architecture + machineType + memory + disk 组合，🟢 简单
- ❌ 待做：ISO 自动检测 — 读取 ISO 头部字节判断 OS 类型（如 El Torito boot record），🟡 中等

---

## 总结：推荐优先级

| 优先级 | 功能 | 难度 | 理由 |
|--------|------|------|------|
| ⭐⭐⭐ | 磁盘管理工具（扩容/压缩/转换） | 🟢 | 纯调用 qemu-img，UI 工作量小，实用性强 |
| ⭐⭐⭐ | 端口转发管理 UI | 🟢 | 模型已有，只差 UI |
| ⭐⭐⭐ | 批量操作 | 🟢 | 改动小，实用价值高 |
| ⭐⭐⭐ | 回收站删除 | 🟢 | 一行代码改动，防误删 |
| ⭐⭐ | 克隆向导 UI | 🟢 | 加一个选择对话框 |
| ⭐⭐ | 搜索过滤增强 | 🟢 | 在现有逻辑加条件 |
| ⭐⭐ | 自动启动 | 🟢 | 简单实用 |
| ⭐⭐ | 后台降低优先级 | 🟢 | 一行代码 |
| ⭐⭐ | 快照管理器 UI | 🟡 | 需要独立视图 |
| ⭐⭐ | 侧边栏分组折叠 | 🟡 | NSTableView group row |
| ⭐⭐ | 操作系统模板 | 🟢 | 预设配置即可 |
| ⭐ | 无缝/融合模式 | 🔴 | 工程量极大 |
| ⭐ | 剪贴板共享 | 🔴 | 需要 spice 协议桥接 |
| ⭐ | USB 直通 | 🔴 | IOKit + 沙盒权限 |
| ⭐ | Web 控制台 | 🔴 | 需内嵌 HTTP server |
| ⭐ | 分辨率自适应 | 🔴 | 依赖 guest agent |
